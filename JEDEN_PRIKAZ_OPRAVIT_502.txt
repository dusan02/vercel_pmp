# 🔧 JEDEN PRÍKAZ - OPRAVIŤ 502 BAD GATEWAY

echo "=== 1. PM2 STATUS ===" && pm2 list | grep -E "(earnings|name|status)" && echo "" && echo "=== 2. PORT 5555 ===" && ss -tlnp | grep 5555 && echo "" && echo "=== 3. TEST LOKÁLNEHO SERVERA ===" && curl -s -o /dev/null -w "HTTP Status: %{http_code}\n" http://127.0.0.1:5555 && echo "" && echo "=== 4. NGINX CONFIG (proxy_pass) ===" && (cat /etc/nginx/sites-enabled/earningstable.com 2>/dev/null || cat /etc/nginx/sites-enabled/default 2>/dev/null || echo "Config not found") | grep -A 2 "proxy_pass" | grep -E "(proxy_pass|5555|3000|app_servers)" && echo "" && echo "=== 5. OPRAVA NGINX ===" && (NGINX_FILE=$(find /etc/nginx/sites-enabled -name "*earningstable*" 2>/dev/null | head -1) && if [ -n "$NGINX_FILE" ]; then sed -i 's|proxy_pass http://app_servers;|proxy_pass http://127.0.0.1:5555;|g' "$NGINX_FILE" && sed -i 's|proxy_pass http://127.0.0.1:3000;|proxy_pass http://127.0.0.1:5555;|g' "$NGINX_FILE" && echo "✅ Nginx config updated: $NGINX_FILE" && nginx -t && systemctl reload nginx && echo "✅ Nginx reloaded" || echo "❌ Nginx reload failed"; else echo "⚠️ Nginx config file not found"; fi) && echo "" && echo "=== 6. FINÁLNY TEST ===" && curl -k -s -o /dev/null -w "HTTPS Status: %{http_code}\n" https://earningstable.com && echo "" && echo "✅ HOTOVO!"

