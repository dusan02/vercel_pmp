cd /var/www/premarketprice && source .env 2>/dev/null && echo "=== 1. KONTROLA PO UPDATE ===" && echo "SELECT symbol, sector, industry FROM \"Ticker\" WHERE symbol IN ('AAPL', 'MSFT', 'GOOGL', 'NVDA', 'META', 'AMZN', 'TSLA', 'TSM', 'BRK.B') ORDER BY symbol;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 2. TEST API ===" && curl -s "http://localhost:3000/api/stocks?tickers=AAPL,MSFT,GOOGL,NVDA" | grep -E '"sector"|"industry"' | head -10 && echo "" && echo "=== 3. TEST API - CELÁ ODPOVEĎ PRE JEDEN TICKER ===" && curl -s "http://localhost:3000/api/stocks?tickers=AAPL" | python3 -m json.tool 2>/dev/null | grep -A 2 -B 2 -E "(sector|industry)" | head -15 || curl -s "http://localhost:3000/api/stocks?tickers=AAPL" | grep -o '"sector":"[^"]*"\|"industry":"[^"]*"' && echo "" && echo "✅ KONTROLA HOTOVÁ"

