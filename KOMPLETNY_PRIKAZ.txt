# 🔧 KOMPLETNÝ PRÍKAZ - Kopírovať a spustiť (ste v /var/www/premarketprice)

# === DIAGNOSTIKA ===
echo "=== 1. Kontrola servera ===" && ss -tlnp | grep 3000 && echo "" && echo "=== 2. Test priameho pripojenia ===" && curl -s http://localhost:3000/api/health | head -3 && echo "" && echo "=== 3. Nginx konfiguračný súbor ===" && nginx -T 2>&1 | grep "configuration file" && echo "" && echo "=== 4. Upstream konfigurácia ===" && nginx -T | grep -A 5 "upstream app_servers" && echo "" && echo "=== 5. Earningstable.com server block ===" && nginx -T | grep -B 5 -A 15 "server_name.*earningstable.com" | head -25 && echo "" && echo "=== 6. Nginx error logy (posledných 20 riadkov) ===" && tail -20 /var/log/nginx/error.log && echo "" && echo "=== 7. Sites-enabled ===" && ls -la /etc/nginx/sites-enabled/ 2>/dev/null && echo "" && echo "=== 8. Test HTTPS ===" && curl -k -s https://earningstable.com/api/health | head -3

