cd /var/www/premarketprice && echo "=== 1. FINÁLNA KONTROLA DATABÁZY ===" && source .env 2>/dev/null && echo "SELECT COUNT(*) as total, COUNT(sector) as with_sector, COUNT(industry) as with_industry, COUNT(*) - COUNT(sector) as missing FROM \"Ticker\";" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 2. PRÍKLADY TICKEROV S HODNOTAMI ===" && echo "SELECT symbol, sector, industry FROM \"Ticker\" WHERE sector IS NOT NULL AND sector != '' ORDER BY symbol LIMIT 20;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 3. TEST API - VYBRANÉ TICKERY ===" && curl -s "http://localhost:3000/api/stocks?tickers=KO,VZ,NEE,ABBV,UBER,CRM,PFE,GE,PG" | grep -o '"sector":"[^"]*"\|"industry":"[^"]*"' | head -18 && echo "" && echo "=== 4. TEST API - GETALL (prvých 10) ===" && curl -s "http://localhost:3000/api/stocks?getAll=true&limit=10" | grep -o '"sector":"[^"]*"\|"industry":"[^"]*"' | head -20 && echo "" && echo "✅ FINÁLNA KONTROLA HOTOVÁ"

