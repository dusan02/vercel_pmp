cd /var/www/premarketprice && source .env 2>/dev/null && echo "=== 1. KONTROLA DATABÁZY - PRIAMY PRÍSTUP ===" && echo "" && echo "Počet tickerov celkom:" && echo "SELECT COUNT(*) FROM \"Ticker\";" | psql "$DATABASE_URL" 2>/dev/null | tail -1 && echo "" && echo "Počet tickerov s NULL/prázdnym sector:" && echo "SELECT COUNT(*) FROM \"Ticker\" WHERE sector IS NULL OR sector = '';" | psql "$DATABASE_URL" 2>/dev/null | tail -1 && echo "" && echo "Počet tickerov s NULL/prázdnym industry:" && echo "SELECT COUNT(*) FROM \"Ticker\" WHERE industry IS NULL OR industry = '';" | psql "$DATABASE_URL" 2>/dev/null | tail -1 && echo "" && echo "=== 2. PRÍKLADY TICKEROV S NULL/PRÁZDNYM SECTOR/INDUSTRY ===" && echo "SELECT symbol, name, sector, industry FROM \"Ticker\" WHERE (sector IS NULL OR sector = '') OR (industry IS NULL OR industry = '') LIMIT 10;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 3. PRÍKLADY TICKEROV S HODNOTAMI ===" && echo "SELECT symbol, name, sector, industry FROM \"Ticker\" WHERE sector IS NOT NULL AND sector != '' AND industry IS NOT NULL AND industry != '' LIMIT 10;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 4. KONTROLA KONKRÉTNYCH TICKEROV (AAPL, MSFT, GOOGL, NVDA, META, AMZN, TSLA) ===" && echo "SELECT symbol, sector, industry FROM \"Ticker\" WHERE symbol IN ('AAPL', 'MSFT', 'GOOGL', 'NVDA', 'META', 'AMZN', 'TSLA') ORDER BY symbol;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "✅ DIAGNOSTIKA HOTOVÁ"

