cd /var/www/premarketprice && echo "=== 1. KONTROLA DATABASE_URL ===" && echo "" && echo "Aktuálny DATABASE_URL (prvých 50 znakov):" && grep DATABASE_URL .env 2>/dev/null | head -1 | cut -c1-50 && echo "..." && echo "" && echo "=== 2. KONTROLA, ČI ZAČÍNA S 'postgresql://' ALEBO 'postgres://' ===" && grep DATABASE_URL .env 2>/dev/null | grep -E "^DATABASE_URL=(postgresql|postgres)://" && echo "✅ DATABASE_URL je správny" || echo "❌ DATABASE_URL nie je správny - treba ho opraviť" && echo "" && echo "=== 3. AK JE NESPRÁVNY, ZOBRAZ CELÝ ===" && echo "Celý DATABASE_URL (bez hesla):" && grep DATABASE_URL .env 2>/dev/null | sed 's/:[^:@]*@/:****@/g' && echo "" && echo "✅ KONTROLA HOTOVÁ"

