# 🔍 JEDEN PRÍKAZ - KOMPLETNÁ DIAGNOSTIKA 502

echo "=== 1. PM2 STATUS ===" && pm2 list | grep -E "(earnings|name|status)" && echo "" && echo "=== 2. PORT 5555 ===" && ss -tlnp | grep 5555 && echo "" && echo "=== 3. TEST LOKÁLNEHO SERVERA ===" && curl -s -o /dev/null -w "HTTP Status: %{http_code}\n" http://127.0.0.1:5555 && echo "" && echo "=== 4. NGINX PROXY PASS ===" && cat /etc/nginx/sites-enabled/earningstable.com 2>/dev/null | grep -A 2 "proxy_pass" || cat /etc/nginx/sites-enabled/default 2>/dev/null | grep -A 2 "earningstable\|5555" || echo "Nginx config not found" && echo "" && echo "=== 5. NGINX ERROR LOG (posledných 10 riadkov) ===" && tail -10 /var/log/nginx/error.log 2>/dev/null || echo "Error log not found" && echo "" && echo "=== 6. PM2 LOGS (posledných 5 riadkov) ===" && pm2 logs earnings-table --lines 5 --nostream 2>/dev/null || echo "No logs"

