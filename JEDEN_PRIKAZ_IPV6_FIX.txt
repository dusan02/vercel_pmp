# 🔧 JEDEN PRÍKAZ - OPRAVA IPv6 + OVERENIE

echo "=== 1. Hľadanie IPv6 listen ===" && grep -r "listen \[::\]" /etc/nginx/sites-enabled/ /etc/nginx/nginx.conf 2>/dev/null && echo "" && echo "=== 2. Odstránenie IPv6 (ak existuje) ===" && find /etc/nginx/sites-enabled/ -name "*premarketprice*" -exec sed -i 's/listen \[::\]:443 ssl;/# listen [::]:443 ssl;/g' {} \; && find /etc/nginx/sites-enabled/ -name "*premarketprice*" -exec sed -i 's/listen \[::\]:80;/# listen [::]:80;/g' {} \; && sed -i 's/listen \[::\]:443 ssl;/# listen [::]:443 ssl;/g' /etc/nginx/nginx.conf 2>/dev/null && sed -i 's/listen \[::\]:80;/# listen [::]:80;/g' /etc/nginx/nginx.conf 2>/dev/null && echo "✅ IPv6 odstránené" && echo "" && echo "=== 3. Overenie server_name ===" && grep -A 5 "listen 443 ssl" /etc/nginx/sites-enabled/* /etc/nginx/nginx.conf 2>/dev/null | grep server_name && echo "" && echo "=== 4. Test Nginx syntax ===" && nginx -t && echo "" && echo "=== 5. Reštart Nginx ===" && systemctl reload nginx && echo "" && echo "=== 6. Overenie SSL certifikátu ===" && certbot certificates | grep -A 5 "premarketprice" && echo "" && echo "=== 7. Test HTTPS ===" && curl -Iv https://premarketprice.com 2>&1 | head -20 && echo "" && echo "✅ HOTOVO!"

