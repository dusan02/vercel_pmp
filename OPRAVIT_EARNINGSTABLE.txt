# 🔧 OPRAVIŤ EARNINGSTABLE.COM - Kopírovať a spustiť (ste v /var/www/premarketprice)

# === KROK 1: Skontrolovať aktuálnu konfiguráciu earningstable.com ===
cat /etc/nginx/sites-enabled/earningstable.com

# === KROK 2: Skontrolovať upstream v tejto konfigurácii ===
grep -A 5 "upstream" /etc/nginx/sites-enabled/earningstable.com

# === KROK 3: Zálohovať pôvodnú konfiguráciu ===
cp /etc/nginx/sites-enabled/earningstable.com /etc/nginx/sites-enabled/earningstable.com.backup

# === KROK 4: Upraviť upstream na 127.0.0.1:3000 ===
# Nájsť riadok s upstream alebo proxy_pass a zmeniť na správnu adresu
sed -i 's|proxy_pass http.*app_servers|proxy_pass http://127.0.0.1:3000|g' /etc/nginx/sites-enabled/earningstable.com
sed -i 's|proxy_pass https.*app_servers|proxy_pass http://127.0.0.1:3000|g' /etc/nginx/sites-enabled/earningstable.com

# Alebo ak je tam upstream blok, upraviť ho:
sed -i 's|server .*:3001|server 127.0.0.1:3000|g' /etc/nginx/sites-enabled/earningstable.com
sed -i 's|server .*:3000|server 127.0.0.1:3000|g' /etc/nginx/sites-enabled/earningstable.com

# === KROK 5: Skontrolovať zmeny ===
grep -E "proxy_pass|upstream|127.0.0.1:3000" /etc/nginx/sites-enabled/earningstable.com

# === KROK 6: Testovať konfiguráciu ===
nginx -t

# === KROK 7: Reload Nginx ===
systemctl reload nginx

# === KROK 8: Testovať ===
curl -k -s https://earningstable.com/api/health | head -5

# Mali by ste dostať JSON (nie 502)

