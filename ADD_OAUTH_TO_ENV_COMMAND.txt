# 🔧 Príkaz na pridanie Google OAuth nastavení do .env súboru

## Možnosť 1: Jednoduchý príkaz (najrýchlejšie)

Spustite tento príkaz na serveri:

```bash
cd /var/www/premarketprice && cat >> .env << 'EOF'

# Google OAuth Configuration
GOOGLE_CLIENT_ID=47392532694-0oi9lef3mj7aoa2159bgmtrmncihvdt1.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE

# NextAuth Configuration
AUTH_SECRET=YOUR_AUTH_SECRET_HERE
NEXTAUTH_URL=https://premarketprice.com
EOF
```

Potom upravte .env súbor:
```bash
nano .env
```

A nahraďte:
- `YOUR_CLIENT_SECRET_HERE` → váš skutočný GOOGLE_CLIENT_SECRET z Google Cloud Console
- `YOUR_AUTH_SECRET_HERE` → váš skutočný AUTH_SECRET (alebo vygenerujte: `openssl rand -base64 32`)

---

## Možnosť 2: S automatickým generovaním AUTH_SECRET

```bash
cd /var/www/premarketprice && cat >> .env << EOF

# Google OAuth Configuration
GOOGLE_CLIENT_ID=47392532694-0oi9lef3mj7aoa2159bgmtrmncihvdt1.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE

# NextAuth Configuration
AUTH_SECRET=$(openssl rand -base64 32)
NEXTAUTH_URL=https://premarketprice.com
EOF
```

⚠️ Poznámka: Tento príkaz automaticky vygeneruje AUTH_SECRET, ale stále musíte manuálne pridať GOOGLE_CLIENT_SECRET.

---

## Možnosť 3: Použitie echo (ak cat nefunguje)

```bash
cd /var/www/premarketprice
echo "" >> .env
echo "# Google OAuth Configuration" >> .env
echo "GOOGLE_CLIENT_ID=47392532694-0oi9lef3mj7aoa2159bgmtrmncihvdt1.apps.googleusercontent.com" >> .env
echo "GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE" >> .env
echo "" >> .env
echo "# NextAuth Configuration" >> .env
echo "AUTH_SECRET=YOUR_AUTH_SECRET_HERE" >> .env
echo "NEXTAUTH_URL=https://premarketprice.com" >> .env
```

---

## Možnosť 4: Použitie tee (ak chcete vidieť výstup)

```bash
cd /var/www/premarketprice
cat << 'EOF' | tee -a .env

# Google OAuth Configuration
GOOGLE_CLIENT_ID=47392532694-0oi9lef3mj7aoa2159bgmtrmncihvdt1.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE

# NextAuth Configuration
AUTH_SECRET=YOUR_AUTH_SECRET_HERE
NEXTAUTH_URL=https://premarketprice.com
EOF
```

---

## Po pridaní - kontrola

```bash
# Skontrolovať, či boli pridané
tail -10 .env

# Mali by ste vidieť nové riadky s GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, atď.
```

---

## Dôležité upozornenia

1. **GOOGLE_CLIENT_SECRET** - Musíte získať z Google Cloud Console:
   - Choďte na Google Cloud Console → APIs & Services → Credentials
   - Kliknite na váš OAuth Client ID
   - V sekcii "Client secrets" buď použite existujúci (ak ho vidíte) alebo vytvorte nový pomocou "+ Add secret"

2. **AUTH_SECRET** - Môže byť:
   - Existujúci secret (ak už máte v .env)
   - Nový vygenerovaný: `openssl rand -base64 32`
   - Musí mať aspoň 32 znakov

3. **NEXTAUTH_URL** - Musí byť presne `https://premarketprice.com` (bez trailing slash)

---

## Po úprave .env súboru

```bash
# Reštartovať aplikáciu
pm2 restart all

# Skontrolovať logy
pm2 logs premarketprice --lines 20
```


