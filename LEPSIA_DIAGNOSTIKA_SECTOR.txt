# 🔍 LEPŠIA DIAGNOSTIKA SECTOR A INDUSTRY (s priamym prístupom k databáze)

cd /var/www/premarketprice && echo "=== 1. KONTROLA DATABÁZY - PRIAMY PRÍSTUP ===" && echo "" && echo "Získanie DATABASE_URL:" && grep DATABASE_URL .env 2>/dev/null | head -1 && echo "" && echo "=== 2. KONTROLA CEZ PSQL (ak je dostupné) ===" && source .env 2>/dev/null && echo "SELECT COUNT(*) as total FROM \"Ticker\";" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "SELECT COUNT(*) as null_sector FROM \"Ticker\" WHERE sector IS NULL OR sector = '';" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "SELECT COUNT(*) as null_industry FROM \"Ticker\" WHERE industry IS NULL OR industry = '';" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 3. PRÍKLADY TICKEROV S NULL/PRÁZDNYM SECTOR/INDUSTRY ===" && echo "SELECT symbol, name, sector, industry FROM \"Ticker\" WHERE (sector IS NULL OR sector = '') OR (industry IS NULL OR industry = '') LIMIT 10;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 4. PRÍKLADY TICKEROV S HODNOTAMI ===" && echo "SELECT symbol, name, sector, industry FROM \"Ticker\" WHERE sector IS NOT NULL AND sector != '' AND industry IS NOT NULL AND industry != '' LIMIT 10;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 5. KONTROLA KONKRÉTNYCH TICKEROV ===" && echo "SELECT symbol, sector, industry FROM \"Ticker\" WHERE symbol IN ('AAPL', 'MSFT', 'GOOGL', 'NVDA', 'META', 'AMZN', 'TSLA') ORDER BY symbol;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "✅ DIAGNOSTIKA HOTOVÁ"

