cd /var/www/premarketprice && source .env 2>/dev/null && echo "=== BACKUP SECTOR/INDUSTRY DÁT ===" && echo "" && echo "Exportujem všetky sector/industry hodnoty do CSV..." && psql "$DATABASE_URL" -c "COPY (SELECT symbol, name, sector, industry FROM \"Ticker\" ORDER BY symbol) TO STDOUT CSV HEADER" > /tmp/sector_industry_backup_$(date +%Y%m%d_%H%M%S).csv && echo "" && echo "✅ Backup vytvorený:" && ls -lh /tmp/sector_industry_backup_*.csv | tail -1 && echo "" && echo "=== POČET TICKEROV S HODNOTAMI ===" && echo "SELECT COUNT(*) as total, COUNT(sector) as with_sector, COUNT(industry) as with_industry FROM \"Ticker\";" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "✅ BACKUP HOTOVÝ"

