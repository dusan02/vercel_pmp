cd /var/www/premarketprice && echo "=== 1. TEST API - CELÁ ODPOVEĎ PRE AAPL ===" && curl -s "http://localhost:3000/api/stocks?tickers=AAPL" | head -50 && echo "" && echo "=== 2. TEST API - HLADANIE SECTOR/INDUSTRY ===" && curl -s "http://localhost:3000/api/stocks?tickers=AAPL" | grep -o '"sector":"[^"]*"' && curl -s "http://localhost:3000/api/stocks?tickers=AAPL" | grep -o '"industry":"[^"]*"' && echo "" && echo "=== 3. KONTROLA, ČI JE SECTOR PRÁZDNY STRING ===" && curl -s "http://localhost:3000/api/stocks?tickers=AAPL" | grep -o '"sector":"[^"]*"' | head -1 && echo "" && echo "=== 4. REŠTART APLIKÁCIE (ak je potrebné) ===" && echo "pm2 restart premarketprice" && echo "" && echo "✅ TEST HOTOVÝ"

