cd /var/www/premarketprice && echo "=== 1. FINÁLNA KONTROLA DATABÁZY ===" && source .env 2>/dev/null && echo "SELECT COUNT(*) as total, COUNT(sector) as with_sector, COUNT(industry) as with_industry, COUNT(*) - COUNT(sector) as missing FROM \"Ticker\";" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 2. PRÍKLADY TICKEROV S HODNOTAMI (prvých 30) ===" && echo "SELECT symbol, sector, industry FROM \"Ticker\" WHERE sector IS NOT NULL AND sector != '' ORDER BY symbol LIMIT 30;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 3. PRÍKLADY TICKEROV BEZ HODNOT (prvých 20) ===" && echo "SELECT symbol, name FROM \"Ticker\" WHERE sector IS NULL OR sector = '' ORDER BY symbol LIMIT 20;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 4. TEST API - VYBRANÉ TICKERY Z TABUĽKY ===" && curl -s "http://localhost:3000/api/stocks?tickers=ASML,BABA,MU,IBM,SAP,GSK,AMGN,BMY,COIN,MSI,MO,TGT,TJX,KR,MDLZ,CL,KDP,BUD,LOW,BKNG,ABNB,MAR,CMG,GM,F,HMC,TM,RACE" | grep -o '"sector":"[^"]*"\|"industry":"[^"]*"' | head -54 && echo "" && echo "✅ FINÁLNA KONTROLA HOTOVÁ"

