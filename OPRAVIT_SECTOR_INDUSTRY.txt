# 🔧 OPRAVA SECTOR A INDUSTRY - NAPLNENIE DÁT

cd /var/www/premarketprice && echo "=== 1. KONTROLA PRED SPUSTENÍM ===" && echo "" && echo "Počet tickerov s NULL/prázdnym sector:" && source .env 2>/dev/null && echo "SELECT COUNT(*) FROM \"Ticker\" WHERE sector IS NULL OR sector = '';" | psql "$DATABASE_URL" 2>/dev/null | tail -1 && echo "" && echo "=== 2. SPUSTENIE BOOTSTRAP STATIC DATA ===" && echo "" && echo "Tento script naplní sector a industry pre všetky tickery:" && npm run db:bootstrap-static && echo "" && echo "=== 3. KONTROLA PO SPUSTENÍ ===" && echo "" && echo "Počet tickerov s NULL/prázdnym sector (po oprave):" && echo "SELECT COUNT(*) FROM \"Ticker\" WHERE sector IS NULL OR sector = '';" | psql "$DATABASE_URL" 2>/dev/null | tail -1 && echo "" && echo "Príklady tickerov s hodnotami (po oprave):" && echo "SELECT symbol, sector, industry FROM \"Ticker\" WHERE symbol IN ('AAPL', 'MSFT', 'GOOGL', 'NVDA', 'META', 'AMZN', 'TSLA') ORDER BY symbol;" | psql "$DATABASE_URL" 2>/dev/null && echo "" && echo "=== 4. TEST API ===" && echo "" && echo "Test API po oprave:" && curl -s "http://localhost:3000/api/stocks?tickers=AAPL,MSFT,GOOGL,NVDA" | grep -o '"sector":"[^"]*"' | head -5 && echo "" && echo "✅ OPRAVA HOTOVÁ"

