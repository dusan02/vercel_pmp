# 🚀 JEDEN PRÍKAZ - Spustiť earnings-cron (ste v /var/www/earnings-table)

cd /var/www/earnings-table && echo "=== 1. Spustenie earnings-cron ===" && pm2 start ecosystem.config.js --only earnings-cron --env production && echo "" && echo "=== 2. Uloženie PM2 konfigurácie ===" && pm2 save && echo "" && echo "=== 3. Čakanie 3 sekundy ===" && sleep 3 && echo "" && echo "=== 4. PM2 status ===" && pm2 status && echo "" && echo "=== 5. Logy earnings-cron (posledných 20 riadkov) ===" && pm2 logs earnings-cron --lines 20 --nostream 2>&1 | tail -20

