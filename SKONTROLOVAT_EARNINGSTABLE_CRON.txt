# 🔍 SKONTROLOVAŤ EARNINGSTABLE.COM - Staré dáta

# === KROK 1: Skontrolovať PM2 procesy ===
pm2 list | grep -E "earnings|name"

# === KROK 2: Skontrolovať, či beží earnings-cron ===
pm2 describe earnings-cron 2>/dev/null || echo "⚠️ earnings-cron nebeží!"

# === KROK 3: Skontrolovať logy earnings-cron ===
pm2 logs earnings-cron --lines 50 --nostream

# === KROK 4: Skontrolovať logy earnings-table ===
pm2 logs earnings-table --lines 30 --nostream

# === KROK 5: Skontrolovať, kedy boli naposledy aktualizované dáta ===
# (závisí od API endpointu)

# === KROK 6: Testovať API endpoint pre refresh ===
curl -X POST http://localhost:5555/api/final-report/refresh 2>&1 | head -10

